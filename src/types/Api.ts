export interface AuthorizeData {
  name: string;
  email: string;
}

export interface UpdateProfileData {
  name: string;
  email: string;
}

export interface CurrentProfileData {
  name: string;
  email: string;
}
